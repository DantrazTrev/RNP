import { View, Text, TextInput } from 'react-native';
import React from 'react';

const Form = ({ navigate }) => {
  return (
    <View>
      <Text>Form</Text>
    </View>
  );
};

export default Form;
